{
  "name": "gpu-profile-selector",
  "version": "25.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "package": "npm run build && cd dist && zip -r ../GPU_profile_selector@lorenzo9904.gmail.com.zip ."
  },
  "devDependencies": {
    "typescript": "~5.7"
  },
  "dependencies": {
    "@girs/gjs": "*",
    "@girs/gnome-shell": "*"
  }
}
